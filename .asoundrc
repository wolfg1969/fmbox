pcm.!default {
    type plug
    slave.pcm {
        type dmix
        ipc_key 1024
        slave {
            pcm "hw:0,0"
            rate 44100
        }
    }
}

audio_output {
    type "alsa"
    name "my ALSA device"
    device "hw:0,0"
    use_mmap "yes"
    auto_resample "no"
}
